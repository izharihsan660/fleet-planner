import DangerButton from '@/Components/DangerButton';
import PrimaryButton from '@/Components/PrimaryButton';
import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
import { PageProps, User } from '@/types';
import { Head, Link, router } from '@inertiajs/react';

export default function Index({ auth, users }: PageProps<{ users: User[] }>) { const destroy = (user: User) => { if (confirm(`Delete user ${user.name}?`)) router.delete(route('users.destroy', user.id)); }; return <AuthenticatedLayout header={<h2 className="text-xl font-semibold leading-tight text-gray-800">Users</h2>}><Head title="Users" /><div className="py-12"><div className="mx-auto max-w-7xl space-y-6 sm:px-6 lg:px-8"><Link href={route('users.create')}><PrimaryButton>Add User</PrimaryButton></Link><div className="overflow-hidden bg-white shadow-sm sm:rounded-lg"><table className="min-w-full divide-y divide-gray-200"><thead className="bg-gray-50"><tr>{['Name','Email','Role','Site','Actions'].map((head) => <th key={head} className="px-6 py-3 text-left text-xs font-medium uppercase text-gray-500">{head}</th>)}</tr></thead><tbody className="divide-y divide-gray-200 bg-white">{users.map((user) => <tr key={user.id}><td className="px-6 py-4 text-sm font-medium text-gray-900">{user.name}</td><td className="px-6 py-4 text-sm text-gray-500">{user.email}</td><td className="px-6 py-4 text-sm text-gray-500">{user.role}</td><td className="px-6 py-4 text-sm text-gray-500">{user.site?.name ?? '-'}</td><td className="space-x-3 px-6 py-4 text-sm"><Link className="text-indigo-600 hover:text-indigo-900" href={route('users.edit', user.id)}>Edit</Link>{auth.user.id !== user.id && <DangerButton onClick={() => destroy(user)}>Delete</DangerButton>}</td></tr>)}</tbody></table></div></div></div></AuthenticatedLayout>; }
