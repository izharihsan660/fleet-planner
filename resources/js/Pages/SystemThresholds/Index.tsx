import DangerButton from '@/Components/DangerButton';
import PrimaryButton from '@/Components/PrimaryButton';
import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
import { PageProps, SystemThreshold } from '@/types';
import { Head, Link, router } from '@inertiajs/react';

export default function Index({ auth, systemThresholds }: PageProps<{ systemThresholds: SystemThreshold[] }>) { const destroy = (threshold: SystemThreshold) => { if (confirm(`Delete ${threshold.key}?`)) router.delete(route('system-thresholds.destroy', threshold.id)); }; return <AuthenticatedLayout header={<h2 className="text-xl font-semibold leading-tight text-gray-800">System Thresholds</h2>}><Head title="System Thresholds" /><div className="py-12"><div className="mx-auto max-w-7xl space-y-6 sm:px-6 lg:px-8"><Link href={route('system-thresholds.create')}><PrimaryButton>Add Threshold</PrimaryButton></Link><div className="overflow-hidden bg-white shadow-sm sm:rounded-lg"><table className="min-w-full divide-y divide-gray-200"><thead className="bg-gray-50"><tr>{['Key','Value','Description','Updated By','Actions'].map((head) => <th key={head} className="px-6 py-3 text-left text-xs font-medium uppercase text-gray-500">{head}</th>)}</tr></thead><tbody className="divide-y divide-gray-200 bg-white">{systemThresholds.map((threshold) => <tr key={threshold.id}><td className="px-6 py-4 text-sm font-medium text-gray-900">{threshold.key}</td><td className="px-6 py-4 text-sm text-gray-500">{threshold.value}</td><td className="px-6 py-4 text-sm text-gray-500">{threshold.description}</td><td className="px-6 py-4 text-sm text-gray-500">{threshold.updated_by?.name ?? '-'}</td><td className="space-x-3 px-6 py-4 text-sm"><Link className="text-indigo-600 hover:text-indigo-900" href={route('system-thresholds.edit', threshold.id)}>Edit</Link><DangerButton onClick={() => destroy(threshold)}>Delete</DangerButton></td></tr>)}</tbody></table></div></div></div></AuthenticatedLayout>; }
