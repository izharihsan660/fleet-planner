import InputError from '@/Components/InputError';
import PrimaryButton from '@/Components/PrimaryButton';
import SecondaryButton from '@/Components/SecondaryButton';
import TextInput from '@/Components/TextInput';
import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
import { PageProps, UnitPlanning, WorkOrder, WorkOrderItem } from '@/types';
import { Head, Link, router, useForm } from '@inertiajs/react';
import { FormEvent, useState } from 'react';

type ResourceItem<T> = { data: T };

function ReasonForm({ title, onCancel, onSubmit, processing, reason, setReason, error }: { title: string; onCancel: () => void; onSubmit: (event: FormEvent) => void; processing: boolean; reason: string; setReason: (reason: string) => void; error?: string }) {
    return <form onSubmit={onSubmit} className="mt-4 space-y-3 rounded-md border border-yellow-200 bg-yellow-50 p-4"><h4 className="font-semibold text-yellow-900">{title}</h4><textarea className="w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500" rows={3} value={reason} placeholder="Alasan" onChange={(event) => setReason(event.target.value)} /><InputError message={error} /><div className="flex gap-2"><PrimaryButton disabled={processing}>Simpan</PrimaryButton><SecondaryButton type="button" onClick={onCancel}>Batal</SecondaryButton></div></form>;
}

function ReplaceForm({ item, workOrderId, onCancel }: { item: WorkOrderItem; workOrderId: number; onCancel: () => void }) {
    const form = useForm({ reason: '' });
    const submit = (event: FormEvent) => { event.preventDefault(); form.post(route('work-orders.items.replace', [workOrderId, item.id]), { preserveScroll: true, onSuccess: onCancel }); };

    return <form onSubmit={submit} className="mt-4 space-y-3 rounded-md border border-blue-200 bg-blue-50 p-4"><h4 className="font-semibold text-blue-900">Submit Replace</h4><textarea className="w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500" rows={3} value={form.data.reason} placeholder="Catatan / alasan opsional" onChange={(event) => form.setData('reason', event.target.value)} /><InputError message={form.errors.reason} /><div className="flex gap-2"><PrimaryButton disabled={form.processing}>Submit Replace</PrimaryButton><SecondaryButton type="button" onClick={onCancel}>Batal</SecondaryButton></div></form>;
}

function PostponeForm({ item, workOrderId, onCancel }: { item: WorkOrderItem; workOrderId: number; onCancel: () => void }) {
    const form = useForm({ reason: '', new_due_km: item.unit_planning?.next_due_km?.toString() ?? '', new_due_date: item.unit_planning?.next_due_date?.slice(0, 10) ?? '' });
    const submit = (event: FormEvent) => { event.preventDefault(); form.post(route('work-orders.items.postpone', [workOrderId, item.id]), { preserveScroll: true, onSuccess: onCancel }); };

    return <form onSubmit={submit} className="mt-4 grid gap-3 rounded-md border border-orange-200 bg-orange-50 p-4 md:grid-cols-3"><div className="md:col-span-3"><h4 className="font-semibold text-orange-900">Submit Postpone</h4><textarea className="mt-2 w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500" rows={3} value={form.data.reason} placeholder="Alasan postpone" onChange={(event) => form.setData('reason', event.target.value)} /><InputError message={form.errors.reason} /></div><div><TextInput className="w-full" type="number" value={form.data.new_due_km} onChange={(event) => form.setData('new_due_km', event.target.value)} /><InputError className="mt-1" message={form.errors.new_due_km} /></div><div><TextInput className="w-full" type="date" value={form.data.new_due_date} onChange={(event) => form.setData('new_due_date', event.target.value)} /><InputError className="mt-1" message={form.errors.new_due_date} /></div><div className="flex gap-2"><PrimaryButton disabled={form.processing}>Submit Postpone</PrimaryButton><SecondaryButton type="button" onClick={onCancel}>Batal</SecondaryButton></div></form>;
}

function CompleteItemForm({ item, workOrderId, currentOdo }: { item: WorkOrderItem; workOrderId: number; currentOdo: number }) {
    const { data, setData, post, processing, errors } = useForm({ completed_odo: currentOdo.toString(), completed_date: new Date().toISOString().slice(0, 10), notes: '' });
    const submit = (event: FormEvent) => { event.preventDefault(); post(route('work-orders.items.complete', [workOrderId, item.id]), { preserveScroll: true }); };

    return <form onSubmit={submit} className="mt-3 grid gap-3 rounded-md bg-gray-50 p-3 md:grid-cols-4"><div><TextInput className="w-full" type="number" value={data.completed_odo} onChange={(event) => setData('completed_odo', event.target.value)} /><InputError message={errors.completed_odo} className="mt-1" /></div><div><TextInput className="w-full" type="date" value={data.completed_date} onChange={(event) => setData('completed_date', event.target.value)} /><InputError message={errors.completed_date} className="mt-1" /></div><div><TextInput className="w-full" value={data.notes} placeholder="Catatan" onChange={(event) => setData('notes', event.target.value)} /><InputError message={errors.notes} className="mt-1" /></div><PrimaryButton disabled={processing}>Complete</PrimaryButton></form>;
}

function BreakdownInspectionForm({ unitId, currentOdo, plannings, onSuccess }: { unitId: number; currentOdo: number; plannings: UnitPlanning[]; onSuccess: () => void }) {
    const form = useForm({ unit_planning_id: plannings[0]?.id?.toString() ?? '', completed_odo: currentOdo.toString() });
    const submit = (event: FormEvent) => { event.preventDefault(); form.post(route('units.breakdown-inspection', unitId), { preserveScroll: true, onSuccess: () => { form.reset(); onSuccess(); } }); };

    return <section className="rounded-lg border border-red-200 bg-red-50 p-4"><h3 className="font-semibold text-red-900">Inspeksi setelah Breakdown</h3><form onSubmit={submit} className="mt-3 grid gap-3 md:grid-cols-3"><select className="rounded-md border-gray-300" value={form.data.unit_planning_id} onChange={(event) => form.setData('unit_planning_id', event.target.value)}>{plannings.map((planning) => <option key={planning.id} value={planning.id}>{planning.planning_item?.name}</option>)}</select><TextInput type="number" value={form.data.completed_odo} onChange={(event) => form.setData('completed_odo', event.target.value)} /><PrimaryButton disabled={form.processing}>Simpan Inspeksi</PrimaryButton></form></section>;
}

export default function Show({ auth, workOrder }: PageProps<{ workOrder: ResourceItem<WorkOrder> }>) {
    const workOrderData = workOrder.data;
    const [blockedItemId, setBlockedItemId] = useState<number | null>(null);
    const [replaceItemId, setReplaceItemId] = useState<number | null>(null);
    const [postponeItemId, setPostponeItemId] = useState<number | null>(null);
    const [showBreakdownForm, setShowBreakdownForm] = useState(false);
    const [showBreakdownInspectionForm, setShowBreakdownInspectionForm] = useState(true);
    const [breakdownInspectionMessage, setBreakdownInspectionMessage] = useState<string | null>(null);
    const hasSubmittedAction = (workOrderData.items ?? []).some((item) => ['pending_create', 'replace', 'postpone'].includes(item.status));
    const canApprove = auth.user.role === 'spv_ops' && hasSubmittedAction;
    const canCondition = ['superadmin', 'admin_site', 'mekanik'].includes(auth.user.role);
    const canSubmitAction = ['superadmin', 'admin_site'].includes(auth.user.role);
    const inspectionPlannings = workOrderData.items?.filter((item) => item.status === 'breakdown').map((item) => item.unit_planning).filter(Boolean) as UnitPlanning[];
    const blockedForm = useForm({ reason: '' });
    const breakdownForm = useForm({ reason: '' });
    const approve = () => router.post(route('work-orders.approve', workOrderData.id), {}, { preserveScroll: true });
    const reject = () => router.post(route('work-orders.reject', workOrderData.id), {}, { preserveScroll: true });
    const markBlocked = (event: FormEvent, itemId: number) => { event.preventDefault(); blockedForm.post(route('work-order-items.blocked', itemId), { preserveScroll: true, onSuccess: () => { blockedForm.reset(); setBlockedItemId(null); } }); };
    const markBreakdown = (event: FormEvent) => { event.preventDefault(); if (workOrderData.unit) { breakdownForm.post(route('units.breakdown', workOrderData.unit.id), { preserveScroll: true, onSuccess: () => { breakdownForm.reset(); setShowBreakdownForm(false); } }); } };

    return <AuthenticatedLayout header={<h2 className="text-xl font-semibold leading-tight text-gray-800">Detail Work Order</h2>}><Head title={`WO #${workOrderData.id}`} /><div className="py-12"><div className="mx-auto max-w-7xl space-y-6 sm:px-6 lg:px-8"><div><Link href={route('work-orders.index')}><SecondaryButton>Kembali</SecondaryButton></Link></div>{breakdownInspectionMessage && <div role="status" className="rounded-lg border border-green-200 bg-green-50 p-4 text-sm font-medium text-green-800">{breakdownInspectionMessage}</div>}<section className="bg-white p-6 shadow-sm sm:rounded-lg"><div className="flex flex-wrap items-start justify-between gap-4"><div><h3 className="text-lg font-semibold text-gray-900">WO #{workOrderData.id} - {workOrderData.unit?.current_plate}</h3><p className="text-sm text-gray-500">{workOrderData.site?.name} · {workOrderData.trigger_type} · {workOrderData.created_at?.slice(0, 10)}</p></div><div className="flex flex-wrap gap-2"><span className="rounded-full bg-indigo-50 px-3 py-1 text-sm font-medium text-indigo-700">{workOrderData.status}</span>{workOrderData.unit?.is_warranty && <span className="rounded-full bg-green-50 px-3 py-1 text-sm font-medium text-green-700">Warranty</span>}{workOrderData.unit?.status === 'breakdown' && <span className="rounded-full bg-red-50 px-3 py-1 text-sm font-medium text-red-700">Breakdown</span>}</div></div><div className="mt-6 flex flex-wrap gap-2">{canApprove && <PrimaryButton onClick={approve}>Approve</PrimaryButton>}{canApprove && <SecondaryButton onClick={reject}>Reject</SecondaryButton>}{canCondition && workOrderData.unit && <SecondaryButton onClick={() => setShowBreakdownForm(true)}>Breakdown</SecondaryButton>}</div>{showBreakdownForm && <ReasonForm title="Tandai Unit Breakdown" reason={breakdownForm.data.reason} setReason={(reason) => breakdownForm.setData('reason', reason)} error={breakdownForm.errors.reason} processing={breakdownForm.processing} onCancel={() => setShowBreakdownForm(false)} onSubmit={markBreakdown} />}</section>{showBreakdownInspectionForm && workOrderData.unit?.status === 'active' && inspectionPlannings.length > 0 && <BreakdownInspectionForm unitId={workOrderData.unit.id} currentOdo={workOrderData.unit.current_odo} plannings={inspectionPlannings} onSuccess={() => { setShowBreakdownInspectionForm(false); setBreakdownInspectionMessage('Inspeksi breakdown tersimpan, cycle lanjut normal.'); }} />}<section className="space-y-4">{workOrderData.items?.map((item) => { const canComplete = canCondition && item.status === 'in_progress'; const canBlock = canCondition && ['on_hold', 'in_progress'].includes(item.status); const canReplaceOrPostpone = canSubmitAction && ['on_hold', 'blocked'].includes(item.status); return <article key={item.id} className="bg-white p-6 shadow-sm sm:rounded-lg"><div className="flex flex-wrap items-start justify-between gap-4"><div><h4 className="font-semibold text-gray-900">{item.planning_item?.name}</h4><p className="text-sm text-gray-500">Due KM {item.unit_planning?.next_due_km?.toLocaleString() ?? '-'} · Due Date {item.unit_planning?.next_due_date ?? '-'}</p>{item.reason && <p className="mt-1 text-sm text-gray-500">Alasan: {item.reason}</p>}{item.status === 'pending_create' && <p className="mt-1 text-sm text-blue-700">Pengajuan Buat Task Sekarang menunggu approval SPV.</p>}{item.status === 'blocked' && <p className="mt-1 text-sm text-yellow-700">Resolve dengan memilih Submit Replace atau Submit Postpone.</p>}{item.status === 'postpone' && <p className="mt-1 text-sm text-orange-700">Pengajuan due baru: KM {item.new_due_km?.toLocaleString() ?? '-'} · {item.new_due_date ?? '-'}</p>}{item.status === 'postponed' && <p className="mt-1 text-sm text-orange-700">Postpone disetujui ke KM {item.new_due_km?.toLocaleString() ?? '-'} · {item.new_due_date ?? '-'}</p>}</div><div className="flex flex-wrap gap-2"><span className="rounded-full bg-gray-100 px-3 py-1 text-sm text-gray-700">{item.status}</span>{item.status === 'pending_create' && <span className="rounded-full bg-blue-50 px-3 py-1 text-sm font-medium text-blue-700">Menunggu Approval</span>}{item.status === 'replace' && <span className="rounded-full bg-blue-50 px-3 py-1 text-sm font-medium text-blue-700">Menunggu Approve Replace</span>}{item.status === 'postpone' && <span className="rounded-full bg-orange-50 px-3 py-1 text-sm font-medium text-orange-700">Menunggu Approve Postpone</span>}{item.status === 'blocked' && <span className="rounded-full bg-yellow-50 px-3 py-1 text-sm font-medium text-yellow-700">Blocked</span>}{item.status === 'breakdown' && <span className="rounded-full bg-red-50 px-3 py-1 text-sm font-medium text-red-700">Breakdown</span>}</div></div>{item.completed_odo && <p className="mt-3 text-sm text-gray-600">Selesai pada {item.completed_date} di KM {item.completed_odo.toLocaleString()}</p>}<div className="mt-3 flex flex-wrap gap-2">{canReplaceOrPostpone && <PrimaryButton onClick={() => setReplaceItemId(item.id)}>Submit Replace</PrimaryButton>}{canReplaceOrPostpone && <SecondaryButton onClick={() => setPostponeItemId(item.id)}>Submit Postpone</SecondaryButton>}{canBlock && <SecondaryButton onClick={() => setBlockedItemId(item.id)}>Blocked</SecondaryButton>}</div>{replaceItemId === item.id && <ReplaceForm item={item} workOrderId={workOrderData.id} onCancel={() => setReplaceItemId(null)} />}{postponeItemId === item.id && <PostponeForm item={item} workOrderId={workOrderData.id} onCancel={() => setPostponeItemId(null)} />}{blockedItemId === item.id && <ReasonForm title="Tandai Item Blocked" reason={blockedForm.data.reason} setReason={(reason) => blockedForm.setData('reason', reason)} error={blockedForm.errors.reason} processing={blockedForm.processing} onCancel={() => setBlockedItemId(null)} onSubmit={(event) => markBlocked(event, item.id)} />}{canComplete && workOrderData.unit && <CompleteItemForm item={item} workOrderId={workOrderData.id} currentOdo={workOrderData.unit.current_odo} />}</article>; })}{(!workOrderData.items || workOrderData.items.length === 0) && <div className="bg-white p-6 text-center text-sm text-gray-500 shadow-sm sm:rounded-lg">Belum ada item.</div>}</section></div></div></AuthenticatedLayout>;
}
